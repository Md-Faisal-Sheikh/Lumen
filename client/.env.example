# Optional — the localhost defaults below are used automatically if omitted.
# Set these when the API/WebSocket server runs somewhere other than localhost:4000.
VITE_API_URL=http://localhost:4000
VITE_WS_URL=ws://localhost:4000
